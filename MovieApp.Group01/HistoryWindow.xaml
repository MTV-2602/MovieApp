<Window x:Class="MovieApp.Group01.HistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MovieApp.Group01"
        mc:Ignorable="d"
        Title="Watch History" Height="800" Width="1400"
        WindowStartupLocation="CenterScreen"
        Background="White" ResizeMode="CanResize"
        Loaded="Window_Loaded">

    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryBrush" Color="#8B3C46"/>
        <SolidColorBrush x:Key="SecondaryBrush" Color="#EC9FAB"/>
        <Style x:Key="NavMenuButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="20,15"/>
            <Setter Property="Foreground" Value="#8B3C46"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="12" Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#EC9FAB"/>
                                <Setter Property="Foreground" Value="#370808"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="NavMenuButtonSelected" TargetType="Button" BasedOn="{StaticResource NavMenuButton}">
            <Setter Property="Background" Value="#D97E8A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style x:Key="HistoryItemBorder" TargetType="Border">
            <Setter Property="Background" Value="#FFF0F3"/>
            <Setter Property="CornerRadius" Value="15"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="BorderBrush" Value="#EC9FAB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FFE4E9"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#E7C0BC" BorderBrush="#EC9FAB" BorderThickness="0,0,2,0" CornerRadius="20" Margin="-20,0,0,0">
            <StackPanel Margin="50,40,20,40">
                <TextBlock Text="VisionTrail" FontSize="30" FontWeight="Bold" Foreground="#8B3C46" Margin="0,0,0,40"/>
                <TextBlock Text="Menu" FontSize="18" FontWeight="Bold" Margin="0,0,0,15" Foreground="#8B3C46"/>

                <Button Style="{StaticResource NavMenuButton}" Click="HomeButton_Click">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🏠" FontSize="20" Margin="0,0,12,0"/>
                        <TextBlock Text="Home"/>
                    </StackPanel>
                </Button>
                <Button Style="{StaticResource NavMenuButton}" Click="WatchlistButton_Click">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🌟" FontSize="22" Margin="0,0,12,0"/>
                        <TextBlock Text="Watchlist"/>
                    </StackPanel>
                </Button>
                <Button Style="{StaticResource NavMenuButton}" Click="ProfileButton_Click">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="👤" FontSize="20" Margin="0,0,12,0"/>
                        <TextBlock Text="Profile"/>
                    </StackPanel>
                </Button>
                <Button Style="{StaticResource NavMenuButtonSelected}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🕒" FontSize="20" Margin="0,0,12,0"/>
                        <TextBlock Text="History"/>
                    </StackPanel>
                </Button>

                <Border Height="1" Background="#EC9FAB" Margin="0,20,0,20"/>
                <Button Style="{StaticResource NavMenuButton}" Click="LogoutButton_Click">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🚪" FontSize="20" Margin="0,0,12,0"/>
                        <TextBlock Text="Logout"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Border>

        <Grid Grid.Column="1" Margin="40,30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Watch History" FontSize="36" FontWeight="Bold" Foreground="#8B3C46" Margin="0,0,0,30"/>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="HistoryListPanel"/>
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>