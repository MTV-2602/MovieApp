<Window
    x:Class="MovieApp.Group01.AdminCommentsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Comments And Ratings - Admin"
    Width="900"
    Height="700"
    Background="White"
    ResizeMode="CanResize"
    WindowStartupLocation="CenterOwner"
    mc:Ignorable="d">
    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryBrush" Color="#8B3C46" />
        <SolidColorBrush x:Key="SecondaryBrush" Color="#EC9FAB" />
        <SolidColorBrush x:Key="PrimaryTextBrush" Color="#370808" />

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="#EC9FAB" />
            <Setter Property="BorderThickness" Value="1.5" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Padding" Value="15" />
            <Setter Property="Margin" Value="0,0,0,10" />
        </Style>

        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}"
                            CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock
            x:Name="MovieTitleText"
            Grid.Row="0"
            Margin="0,0,0,20"
            FontSize="24"
            FontWeight="Bold"
            Foreground="#8B3C46"
            Text="Comments And Ratings" />

        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border
                Grid.Column="0"
                Margin="0,0,10,0"
                Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock
                        Margin="0,0,0,10"
                        FontSize="16"
                        FontWeight="SemiBold"
                        Foreground="#370808"
                        Text="Rating Statistics" />
                    <TextBlock
                        x:Name="AverageRatingText"
                        FontSize="32"
                        FontWeight="Bold"
                        Foreground="#8B3C46" />
                    <TextBlock
                        x:Name="RatingCountText"
                        Margin="0,5,0,0"
                        FontSize="14"
                        Foreground="#666" />
                </StackPanel>
            </Border>

            <Border
                Grid.Column="1"
                Margin="10,0,0,0"
                Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock
                        Margin="0,0,0,10"
                        FontSize="16"
                        FontWeight="SemiBold"
                        Foreground="#370808"
                        Text="Comment Statistics" />
                    <TextBlock
                        x:Name="TotalCommentsText"
                        FontSize="32"
                        FontWeight="Bold"
                        Foreground="#8B3C46" />
                    <TextBlock
                        Margin="0,5,0,0"
                        FontSize="14"
                        Foreground="#666"
                        Text="Total comments" />
                </StackPanel>
            </Border>
        </Grid>

        <Border
            Grid.Row="2"
            Margin="0"
            Style="{StaticResource CardStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock
                    Grid.Row="0"
                    Margin="0,0,0,15"
                    FontSize="18"
                    FontWeight="SemiBold"
                    Foreground="#370808"
                    Text="Comments" />

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel x:Name="CommentsPanel" />
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</Window>

