<Page
    x:Class="MovieApp.Group01.DirectorManagementPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Loaded="Page_Loaded">
    <Page.Resources>

        <SolidColorBrush x:Key="PrimaryBrush" Color="#8B3C46" />
        <SolidColorBrush x:Key="SecondaryBrush" Color="#EC9FAB" />
        <SolidColorBrush x:Key="PrimaryTextBrush" Color="#370808" />

        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Padding" Value="15,8" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}"
                            CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="1.05" ScaleY="1.05" />
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="RenderTransformOrigin" Value="0.5,0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AdminDataGrid" TargetType="DataGrid">
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="#EC9FAB" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="RowBackground" Value="White" />
            <Setter Property="AlternatingRowBackground" Value="#FDEFF1" />
            <Setter Property="GridLinesVisibility" Value="Horizontal" />
            <Setter Property="HorizontalGridLinesBrush" Value="#EC9FAB" />
            <Setter Property="VerticalGridLinesBrush" Value="Transparent" />
            <Setter Property="HeadersVisibility" Value="Column" />
            <Setter Property="SelectionMode" Value="Single" />
            <Setter Property="SelectionUnit" Value="FullRow" />
            <Setter Property="CanUserAddRows" Value="False" />
            <Setter Property="CanUserDeleteRows" Value="False" />
            <Setter Property="AutoGenerateColumns" Value="False" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="RowHeight" Value="40" />
        </Style>

    </Page.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock
                FontSize="36"
                FontWeight="Bold"
                Foreground="#8B3C46"
                Text="Director Management" />

            <Border
                Grid.Column="1"
                Padding="20,9"
                Background="White"
                BorderBrush="#EC9FAB"
                BorderThickness="2"
                CornerRadius="25">
                <StackPanel Orientation="Horizontal">
                    <TextBlock
                        Margin="0,0,10,0"
                        FontSize="18"
                        Foreground="#8B3C46"
                        Text="🔍" />
                    <TextBox
                        x:Name="SearchBox"
                        Width="250"
                        Background="Transparent"
                        BorderThickness="0"
                        FontSize="14"
                        Foreground="#8B3C46"
                        TextChanged="SearchBox_TextChanged" />
                </StackPanel>
            </Border>
        </Grid>

        <StackPanel
            Grid.Row="1"
            Margin="0,0,0,20"
            Orientation="Horizontal">
            <Button
                x:Name="AddButton"
                Click="AddButton_Click"
                Style="{StaticResource ActionButton}"
                Background="#4CAF50"
                Content="➕ Add Director"
                Margin="0,0,10,0" />
            <Button
                x:Name="EditButton"
                Click="EditButton_Click"
                Style="{StaticResource ActionButton}"
                Background="#2196F3"
                Content="✏️ Edit"
                Margin="0,0,10,0"
                IsEnabled="False" />
            <Button
                x:Name="DeleteButton"
                Click="DeleteButton_Click"
                Style="{StaticResource ActionButton}"
                Background="#F44336"
                Content="❌ Delete"
                Margin="0,0,10,0"
                IsEnabled="False" />
            <Button
                x:Name="RefreshButton"
                Click="RefreshButton_Click"
                Style="{StaticResource ActionButton}"
                Background="#FF9800"
                Content="🔄 Refresh"
                Margin="0,0,10,0" />
        </StackPanel>

        <DataGrid
            x:Name="DirectorsDataGrid"
            Grid.Row="2"
            Style="{StaticResource AdminDataGrid}"
            SelectionChanged="DirectorsDataGrid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding DirectorId}" Header="ID" Width="60" />
                <DataGridTextColumn Binding="{Binding DirectorName}" Header="Name" Width="200" />
                <DataGridTextColumn Binding="{Binding Country}" Header="Country" Width="150" />
                <DataGridTextColumn Binding="{Binding Status}" Header="Status" Width="100" />
                <DataGridTextColumn Binding="{Binding Bio}" Header="Bio" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>

